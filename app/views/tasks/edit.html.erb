<h2>Modify a task</h2>

<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <%= f.submit 'Edit' %>
<% end %>

<%= link_to 'Go back to tasks list', index_path %>
